# Контент-агент канала Cucumber Game

Полные инструкции — в `AGENT.md`. Прочитай его целиком и следуй строго, включая порядок шагов (ШАГ 0 → ШАГ 7).

Железное, чтобы не потерялось:

- **ШАГ 0 (дедуп) — первым делом**, до выбора повода.
- **Запись в дедуп и пуш в `main` — ДО публикации** (ШАГ 4 раньше ШАГ 5). Не записалось — не постить.
- Автопубликация только в тестовый канал. В мейн — исключительно через апрув.
- Нет достойного не-дубля — нормальный исход: СКИП в `decisions.log`, запушить, закончить.
- Пуш в `main` напрямую, пул-реквесты не создавать.

Окружение: Python 3 + Pillow (баннер), шрифты DejaVu/Liberation. Ставится через `setup.sh`.
